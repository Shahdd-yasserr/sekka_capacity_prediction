[phases.setup]
nixPkgs = ["gcc", "gfortran", "gomp", "openblas", "libstdcxx5"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmds = ["uvicorn api.api:app --host 0.0.0.0 --port $PORT"]
